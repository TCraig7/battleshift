Hi <%= @user.name %>,

Welcome to BattleShift! Click on the link below to activate your account:

<%= ENV["path"] + "/activate/#{@user.id}?key=#{@user.activation_digest}" %>

Below is your API key. Please keep this key in a secure location.

<%= @user.api_key %>
